{
  "name": "auto-game",
  "version": "1.0.0",
  "private": true,
  "description": "Auto Game - AI-driven game development project",
  "scripts": {
    "dev": "pnpm --filter @auto-game/pages dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "keywords": [
    "game",
    "ai",
    "automation"
  ],
  "author": "towersxu",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
